# Spend import review

An AP export has four rows that require human attention. First let a finance operator review field mappings and preview the changes before saving the import. Then make uncertain vendor matches explicit, with the source row and the bounded next action available together. The save outcome should state what was imported, what remains in review, and preserve the original transaction as source evidence.
