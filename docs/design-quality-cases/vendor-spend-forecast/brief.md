# Vendor spend forecast review

Review a synthetic vendor forecast before a renewal. Keep actuals, expected charges, commitments, and assumptions distinct; the core job is to make the variance actionable. This is an advisory future-state evaluation, not proof of an implemented forecasting decision contract.
