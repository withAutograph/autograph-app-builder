# Sticky-action experiment: not accepted as an improvement

The subjective score fell from 90 to **80/100**. Typography and product clarity
remain 4/4, but initial captures show supporting content beneath the sticky
footer. A focused browser check proved the action visible and the final fact
reachable after scrolling at 1024 and 700 pixels; that narrow interaction result
does not establish good visual composition. All 20 routine fixture checks passed.

This experiment is retained rather than omitted or re-scored unchanged. The
next candidate removes sticky behavior in favor of a non-overlapping supported
action placement. No palette, rubric, or scoring threshold changed.

Adherence fell to **99, partial** because the generated source supplied an
unsupported `RecordList.description` prop. The text did not render; the
evaluator correctly records an API violation rather than awarding credit for an
unused string. This will be corrected through an actual supported component.

The spacing extraction repair improves authored shorthand diagnostics but
does not turn unknown stylesheet ownership into generated styling credit.
Styling coverage remains 0.26%.
