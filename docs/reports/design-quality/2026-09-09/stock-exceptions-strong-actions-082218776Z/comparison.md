# Strong action comparison

The opt-in shared `primary-strong` Button restored both hierarchy and typography to 4/4. It uses existing Arrusted action-primary-ink and inverse-text tokens; their measured pairing is 5.58:1. The palette and existing primary default are unchanged. Shared implementation: [Arrusted PR #1319](https://github.com/withAutograph/arrusted-development/pull/1319).

The overall advisory score is **90/100**, not 100. This judgment rated layout and responsive composition 3/4, identifying the existing header/workspace alignment offset and the preselected narrow initial state. Prior reports had rated those unchanged dimensions 4/4. Preserve this result as evidence of both the concrete tradeoff improvement and subjective judgment variation.

The next correction aligns the heading with the workspace without changing the approved Back navigation or workspace width. A score does not justify silently changing an approved interaction pattern.

No scoring weights, palette values, runtime gates, or generated visual overrides were introduced. Historical comparison reports remain unchanged.
