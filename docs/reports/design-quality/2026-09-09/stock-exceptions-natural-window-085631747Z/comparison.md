# Natural-height comparison

The advisory score remains **90/100**. Removing the short panel cap at 1024px avoids hiding supplier facts, but moves the result footer below the initial viewport. At wider sizes the nearly-fitting internal scroll region remains. This is a real tradeoff, not a score improvement. All original captures and the unchanged judge result are retained.

The next coherent composition pass will use the existing shared capabilities for a constrained list-first entry, grouped decision quantities, and one page scroll owner. It must retain wide selection, visible product explanations, the approved Back route, the Arrusted palette, and fixture-only actions.

No benchmark threshold or runtime gate follows from these advisory findings. The checked narrow Back route exposes the filter section and all four records; an earlier test accidentally selected a hidden native option instead of the visible custom Select and is not evidence of a UI failure.

Static evidence work is separate: the corrected finite JSX alias analysis assesses the `list` and `detail` public props without claiming they rendered. Its controlled replay preserves reliable scalar evidence and leaves dynamic callbacks and browser styling provenance unassessed.
