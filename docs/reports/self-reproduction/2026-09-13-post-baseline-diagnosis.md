# Shared Builder repairs after the public baseline

The original public baseline remains failed delivery. This diagnosis uses read-only internal trace inspection after generation ended; it is not additional generation evidence. No candidate files were changed and no internal operation was used to continue the baseline.

## Confirmed diagnosis

Session `wrun_01M2DTBRT44GXGANVH26SHNBS1` applied 33 changed files at `2026-09-13T16:52:55.335Z` in its private Sandbox. Two technical commands passed at `16:53:20.961Z`; the validation result explicitly reported product behavior as unassessed with no evidence. The configured empty output directory is a publication destination, not the Sandbox implementation directory.

The second apply request supplied draft and creation actions that returned a revision, synthetic queued run ID, and cancelled state without persistence or orchestration. The provider-return route returned `connected-emulated` JSON without persisting the callback. These observed stubs do not satisfy the corresponding backend requirements. The complete generated source has not been audited.

The accepted change set included `.next/BUILD_ID`, manifests, chunks, and maps. No working-app preview startup appeared in the inspected action-result sequence. The public placeholder localhost URL still does not establish delivery.

## Repair coverage

| Gap                                      | Shared change                                                                                                                                                 | Remaining proof                                                                                                                                            |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Technical tool name on creation approval | Map the actual `apply_app_creation` runtime name to “Build this app?” and its product description                                                             | New public-flow observation; duplicate approvals remain unresolved                                                                                         |
| Build artifacts in reviewed changes      | Omit default `.next` directories when snapshotting application overlays, including nested apps                                                                | Custom build directories and previously recorded receipts are not covered by this repair                                                                   |
| Unsupported readiness and backend claims | Shared instructions distinguish apply, technical validation, product behavior, prototype, and working delivery; turn context repeats actual phase limitations | Guidance is not deterministic product validation and does not implement missing backend behavior                                                           |
| No working app handoff                   | Still open                                                                                                                                                    | Shared runtime startup, reachable URL, owner access, expiry, recovery, and lifecycle; fixture previews earn no credit                                      |
| Synthetic backend behavior               | Still open                                                                                                                                                    | Durable authenticated writes and independent orchestration, tested through real product actions                                                            |
| Unassessed replica quality               | Still open                                                                                                                                                    | After working delivery, exercise persistence/reload, callbacks, cancellation/retry/recovery, child creation, paired desktop states, and framework behavior |
| Starter cloning                          | Still open                                                                                                                                                    | Observe normal Builder remote cloning and installation in a new session; the original used an existing checkout                                            |

## Validation and next acceptance

Focused tests exercise the actual overlay snapshot script against nested Next build output and source files, public approval projection with the runtime tool name, and completion guidance across phases. TypeScript and Eve compilation validate the instruction integration. These checks establish repairs to shared behavior, not a successful replica.

A future acceptance run must start a new session through the public entrypoint with the product brief and ordinary answers/approvals only. Preserve the original baseline and record the new source revision. Do not launch the old candidate internally to improve its score. Missing product functionality remains failed; inaccessible infrastructure remains blocked; unobserved behavior remains unassessed.

A separate-origin working-preview access prototype was investigated outside the repository. It is not integrated or delivered. Public port exposure alone is insufficient: owner delivery, lifecycle, recovery, and browser behavior must be resolved before that work can close the handoff gap. No eval-specific hosting is required or authorized by this diagnosis.
