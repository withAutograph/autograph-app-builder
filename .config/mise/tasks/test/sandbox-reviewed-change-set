#!/bin/sh
#MISE description="Run the shared Vercel Sandbox sandbox-reviewed-change-set integration check"
set -eu
if [ "$#" -ne 1 ]; then
  echo "usage: mise run test:sandbox-reviewed-change-set -- <arrusted-source-root>" >&2
  exit 64
fi
exec mise run test:sandbox-evals -- "$1" sandbox-reviewed-change-set
