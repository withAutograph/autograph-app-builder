#!/usr/bin/env bash
#MISE description="Prove the pinned sandbox toolchain through Eve"
set -euo pipefail

node_bin="$(mise which node)"
export APP_BUILDER_TEST_MODEL=1
export APP_BUILDER_REAL_SANDBOX=1
exec "$node_bin" node_modules/eve/bin/eve.js eval sandbox-toolchain --strict --skip-report
