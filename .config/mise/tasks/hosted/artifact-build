#!/bin/sh
#MISE description="Build the immutable hosted Arrusted dependency artifact"
set -eu

exec "$PWD/.config/mise/scripts/trusted-node-launcher" \
  "$(mise which node)" \
  .config/mise/scripts/repository/build-hosted-arrusted-artifact.mts \
  "$@"
