#!/bin/sh
#MISE hide=true
#MISE description="Create the exact frozen-install receipt used only by the sandbox proof"
set -eu

node_bin="$(mise which node)"
export APP_BUILDER_IMAGE_NODE_BIN="$node_bin"
export APP_BUILDER_IMAGE_PNPM_BIN="$(mise which pnpm)"
exec "$PWD/.config/mise/scripts/trusted-node-launcher" "$node_bin" --experimental-strip-types .config/mise/scripts/repository/image-lifecycle.ts prepare-proof-runtime "$@"
