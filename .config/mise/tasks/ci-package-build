#!/bin/sh
#MISE description="Build and verify Eve, packages, generated manifests, and the app for CI"
set -eu

mise run agent:info
mise run agent:build
mise run package:build
mise run package:build-openai
mise run package:verify-generated
exec mise run app:build
