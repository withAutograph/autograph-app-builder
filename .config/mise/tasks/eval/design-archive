#!/bin/sh
#MISE description="Archive a reviewed design report and screenshots in Git"
set -eu
exec .config/mise/scripts/trusted-node-launcher \
  "$(mise which node)" --import tsx scripts/design-quality/archive.mts "$@"
