#!/bin/sh
#MISE hide=true
#MISE description="Test deterministic release and portable client adapters"
set -eu

exec .config/mise/scripts/trusted-node-launcher "$(mise which node)" --import tsx scripts/test-portable-release.mts
