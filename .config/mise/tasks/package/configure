#!/bin/sh
#MISE description="Configure derived client manifests for an approved origin"
set -eu

exec .config/mise/scripts/trusted-node-launcher "$(mise which node)" --import tsx scripts/configure-template.mts "$@"
