#!/bin/sh
#MISE hide=true
#MISE description="Validate install-ready Agent Plugins release configuration"
set -eu

exec .config/mise/scripts/trusted-node-launcher "$(mise which node)" --import tsx scripts/validate-plugin.mts --release
