#!/bin/sh
#MISE description="Check repository formatting with the pinned runtime"
set -eu

exec .config/mise/scripts/trusted-node-launcher \
  "$(mise which node)" node_modules/prettier/bin/prettier.cjs --check .
