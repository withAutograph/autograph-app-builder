#!/bin/sh
#MISE description="Formatting checks deferred to follow-up"
set -eu

# The repository-wide Oxfmt migration is intentionally deferred. Keep the
# formatter task available for the follow-up without rewriting existing files.
exit 0
