diff --git a/dist/index.js b/dist/index.js
index 391e8f4ed01ede21a6eab9fed5a064b2f179489a..cc5f0cbc4efadc707fa82229d73e896131024d00 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -741,11 +741,6 @@ function debug(label, ...args) {
   }
 }
 var __dirname = dirname(fileURLToPath(import.meta.url));
-var FONTS = {
-  "geist-sans.woff2": readFileSync(join(__dirname, "fonts", "geist-sans.woff2")),
-  "GeistPixel-Square.woff2": readFileSync(join(__dirname, "fonts", "GeistPixel-Square.woff2"))
-};
-var FAVICON = readFileSync(join(__dirname, "fonts", "favicon.ico"));
 function parsePagination(c) {
   const page = Math.max(1, parseInt(c.req.query("page") ?? "1", 10) || 1);
   const per_page = Math.min(100, Math.max(1, parseInt(c.req.query("per_page") ?? "30", 10) || 30));
