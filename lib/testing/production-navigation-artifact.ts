// The production-navigation runner changes only its disposable source snapshot.
// Environment variables must never activate this capability in deployed builds.
export const productionNavigationArtifact = false;
